{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Taskforce settings and signal windows",
  "windows": ["settings", "overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
